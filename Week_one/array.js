const cars = ["toyota", "honda", "hyundai"]

for(let y=0; y < cars.length; y++){
    const car = cars[y];
    if (car == "hyundai"){

        console.log("not made in japan");
    }
    
}
//forEach loop
cars.forEach (cars);{
    console.log(cars);
if (car == "hyundai") {
    console.log("not made in japan")
    break;
}
}
