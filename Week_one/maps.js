let mp=new Map()
     
// Adding values to the map
 //add x y z
mp.set("a",65);
mp.set("b",66);
mp.set("x",67);
mp.set("y",68);
mp.set("z"69);
 
  // delete a
mp.delete(a);
// Logging map object
//mp.forEach((values,keys)=>{
  console.log("values: ",values+
                 ", keys: ",keys+ "<br>")
})
